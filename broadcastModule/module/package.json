{
  "name": "broadcast-module",
  "version": "1.0.0",
  "description": "example of module which allow to send messages to other modules",
  "main": "index.js",
  "scripts": {
    "transpile": "ngc",
    "package": "rollup -c",
    "minify": "uglifyjs dist/bundles/broadcastModule.umd.js --screw-ie8 --compress --mangle --comments --output dist/bundles/broadcastModule.umd.js",
    "build": "npm run transpile && npm run package && npm run minify"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@angular/common": "^5.0.2",
    "@angular/core": "^5.0.2",
    "rxjs": "^5.5.2",
    "zone.js": "^0.8.18"
  },
  "devDependencies": {
    "@angular/compiler": "^5.0.2",
    "@angular/compiler-cli": "^5.0.2",
    "rollup": "^0.51.7",
    "typescript": "^2.4.2",
    "uglify-js": "^3.1.9"
  }
}
