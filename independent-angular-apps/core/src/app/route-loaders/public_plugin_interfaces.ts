import { ProjectTwoPluginManager } from 'project-two';
import { VoicePluginManager } from 'project-one';

export const pluginManagers = {
    files: ProjectTwoPluginManager, voice: VoicePluginManager
};
